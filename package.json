{
  "name": "kicknap",
  "version": "0.1.0",
  "private": true,
  "workspaces": [
    "web",
    "services/*"
  ],
  "scripts": {
    "dev": "concurrently -n web,listings,availability,bookings,identity -c blue,green,magenta,yellow,cyan \"npm run dev -w web\" \"npm run dev -w services/listings\" \"npm run dev -w services/availability\" \"npm run dev -w services/bookings\" \"npm run dev -w services/identity\"",
    "build": "npm run build -w web",
    "lint:arch": "depcruise -c .dependency-cruiser.mjs web/src services",
    "check": "npm run lint:arch && npm run lint -w web && npm run build -w web",
    "db:start": "node scripts/db-start.mjs",
    "db:init": "npm run db:init -w services/listings && npm run db:init -w services/availability && npm run db:init -w services/bookings && npm run db:init -w services/identity",
    "db:seed": "npm run db:seed -w services/listings && npm run db:seed -w services/availability && npm run db:seed -w services/bookings && npm run db:seed -w services/identity"
  },
  "devDependencies": {
    "concurrently": "^10.0.5",
    "dependency-cruiser": "^18.2.0"
  }
}
